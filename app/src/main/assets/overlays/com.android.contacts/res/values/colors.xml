<?xml version="1.0" encoding="utf-8"?>
<resources>
	<!-- Color of the background of the contact detail and editor pages -->
	<color name="background_primary">@*common:drawable/background_holo_light_inverse</color>

    <!-- Color used for the letter in the A-Z section header -->
    <color name="section_header_text_color">@*common:color/grey_light</color>

    <!-- Color of the background of the action bar -->
    <color name="action_bar_background">@*common:color/blue_light</color>

    <!-- Color of the background of the action bar when highlighted (ie. pressed, focused) -->
    <color name="action_bar_background_highlight">@*common:color/blue</color>

    <!--  Color of the vertical stripe that goes on the left of a block quote inside a stream item -->
    <color name="stream_item_stripe_color">@*common:color/blue_light</color>

    <color name="detail_tab_carousel_tab_label_background_color">@*common:color/blue_light</color>
</resources>